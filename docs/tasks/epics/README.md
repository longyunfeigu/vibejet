# Active Epics

No active base-library epics are currently tracked here.

Use this directory for current implementation epics and stories that apply to
the `vibejet` foundation library. Product-specific epics should live in the
downstream application repository or under `docs/archive/` when retained only
for historical context.
