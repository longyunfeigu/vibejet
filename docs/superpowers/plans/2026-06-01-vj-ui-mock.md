# vj-ui-mock Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增一个 repo-local skill `vj-ui-mock`，补上 AI 工作流缺失的「UI 设计生产端」：Phase A 产出全局设计基座 `design_guidelines.md`，Phase B 按 target 产出每屏 v0/Lovable 提示词。

**Architecture:** 两阶段。Phase A 从 PRD + 全部 epic/story + CLAUDE.md（栈）生成 6 节 + `v0 Base Prompt` 段的 `docs/project/design_guidelines.md`（全局一次）。Phase B 把任意 target（epic/story/混合）归并去重成「屏清单」，每屏提示词头部整段拷贝 `v0 Base Prompt` 段实现跨 Epic 衔接，落盘并回填 Story 设计参考。产物是 markdown 提示词，不直接出图。

**Tech Stack:** 纯 markdown skill（提示词），无可执行代码。验证靠对真实 epic（epic-1 登录 / epic-4 考试）做 dry-run，对照 spec §11 验收标准。

**Spec:** `docs/superpowers/specs/2026-06-01-vj-ui-mock-design.md`

**前置（执行时）：** 本仓库铁律——主仓库改动走 worktree。执行本计划前，按 `superpowers:using-git-worktrees` 开 worktree（或经用户确认在 `feat/chuti` 直接改）。每个 Task 末尾的 commit 在该 worktree/分支内进行。

---

## 这是 prose（skill）类计划，"测试"如何做

没有 pytest。每个产物 Task 的验证 = **对照具体检查清单 / 对真实 epic 做 dry-run**，pass 条件写成可勾选项。Task 6 是集中 dry-run，把 spec §11 的 8 条验收标准逐条跑通。

## File Structure

| 动作 | 路径 | 职责 |
|------|------|------|
| Create | `.agents/skills/vj-ui-mock/SKILL.md` | skill 主体：Phase A / Phase B 两阶段流程 |
| Create | `.agents/skills/vj-ui-mock/design_guidelines.template.md` | Phase A 产出模板（6 节 + v0 Base Prompt 段） |
| Create | `.agents/skills/vj-ui-mock/v0-prompt.template.md` | Phase B 每屏提示词产出模板 |
| Modify | `.agents/skills/skill-rules.json` | 注册 vj-ui-mock 激活触发词 |
| Modify | `docs/reference/guides/ai-workflow.md` | 把 vj-ui-mock 接入链路（§2 阶段A、§5 设计图、§9 决策表、§10 默认顺序） |

不新建 `references/`：屏归并逻辑很短，直接写进 SKILL.md Phase B（KISS）。

---

## Task 1: design_guidelines.template.md（Phase A 产出模板）

**Files:**
- Create: `.agents/skills/vj-ui-mock/design_guidelines.template.md`

- [ ] **Step 1: 写模板文件**

完整内容（含 `<!-- 引导 -->` 注释，模板本身不含真实业务数据）：

````markdown
# 设计指南 (Design Guidelines)

> 全局设计基座，由 `vj-ui-mock` Phase A 生成。所有屏的 UI 提示词都继承本文件的 `## v0 Base Prompt` 段，以保证跨 Epic 视觉与导航一致。
> 维护时机：信息架构 / 导航骨架 / 设计系统变化时增量更新。每节短写，够 v0 生成一致 UI 即可。

## 1. 前端栈快照

<!-- 来自 CLAUDE.md，冗余记录以让 v0 提示词自包含。只记事实，不做选型论证。 -->
- 框架：<React 19 + Vite 8>
- 组件库：<MUI v9>
- 路由 / 数据：<TanStack Router / Query>
- 表单 / 校验：<RHF + Zod>

## 2. 信息架构 / 站点地图

<!-- 枚举全部屏。一行一屏。来源 = 各 Story #### 前端验收标准 里的路由。 -->

| 路由 | 屏名 | 角色 | 来源 Epic/Story |
|------|------|------|-----------------|
| `</login>` | <登录页> | <全部> | <epic-1 / 1.1> |
| ... | ... | ... | ... |

## 3. 导航外壳

<!-- 持久布局：所有屏都套这层。这是跨 Epic 衔接的骨架。 -->
- **Header**：<logo + 当前用户名 + 登出>
- **侧边导航**：<按角色分组的菜单项；管理员 vs 员工看到不同项>
- **内容区**：<路由嵌套方式，如 <AppShell> 包裹 <Outlet>>
- **未登录态**：<重定向 /login>

## 4. 设计 tokens

- **配色**：主 `<#hex>` / 辅 `<#hex>` / 语义（成功/警告/错误/信息）`<...>`
- **字体阶**：<H1/H2/Body/Caption 字号与字重>
- **间距**：<基准网格，如 8px>
- **圆角 / 阴影**：<radius / elevation 约定>

## 5. 共享组件 + 状态约定

<!-- 尤其三态：本产品每个 AI 环节都需要 loading / 失败兜底。 -->
- **基础组件用法**：<按钮层级、表单字段、卡片、表格、对话框>
- **空态**：<无数据时的引导文案 + 主操作按钮，不出现空白页>
- **Loading 态**：<短交互 spinner；长 LLM 调用显式"进行中"+ 进度/可取消，不卡死无反馈>
- **错误态**：<错误文案 + 重试入口 + 人工兜底入口（编辑/手动补）>

## 6. 角色 UX 规则

| 角色 | 可访问 | 不可见 | 导航差异 |
|------|--------|--------|----------|
| `<出题管理员>` | <...> | <他人记录> | <...> |
| `<员工考生>` | <仅本人相关> | <他人记录 / 管理功能> | <...> |

## v0 Base Prompt

<!-- 抽 §1/3/4/5 压成的一段工具中立自然语言。Phase B 每屏提示词整段拷贝此块到头部。手调此块即可全局生效。 -->

```
你正在为一个公司内部业务考试平台设计界面。技术目标：用 MUI v9 + React 风格组件实现，输出干净、专业、信息密度适中的内部工具风格（非消费级炫酷）。

全局外壳：每个页面都包含顶部 Header（左 logo、右当前用户名 + 登出），左侧按角色分组的侧边导航；主内容区在右侧。未登录时重定向到登录页。

设计系统：主色 <#hex>，辅色 <#hex>，语义色（成功/警告/错误/信息）见规范；<8px> 间距网格；标题/正文字阶见规范；卡片与表格为主要信息容器。

状态约定（重要）：每个涉及后端或 AI 调用的区域都必须画出三态——空态（引导文案 + 主操作，不留白）、Loading 态（长 AI 调用显式"进行中"且不卡死）、错误态（错误文案 + 重试 + 人工兜底入口）。

请在以上全局约束下，设计下面这个具体页面：
```
````

- [ ] **Step 2: 验证模板**

检查清单（全部勾选才算 pass）：
- [ ] 含全部 6 节标题 + `## v0 Base Prompt`
- [ ] 每节有 `<!-- 引导 -->` 注释说明怎么填
- [ ] §1 标注"来自 CLAUDE.md，冗余记录"
- [ ] §5 显式包含 空态 / Loading / 错误 三态
- [ ] `v0 Base Prompt` 是一个 fenced code block，结尾以"请...设计下面这个具体页面："收口（供 Phase B 拼接）
- [ ] 模板里是占位符（`<...>`），无任何真实业务数据

- [ ] **Step 3: Commit**

```bash
git add .agents/skills/vj-ui-mock/design_guidelines.template.md
git commit -m "feat(vj-ui-mock): add design_guidelines template for Phase A"
```

---

## Task 2: v0-prompt.template.md（Phase B 每屏产出模板）

**Files:**
- Create: `.agents/skills/vj-ui-mock/v0-prompt.template.md`

- [ ] **Step 1: 写模板文件**

完整内容：

````markdown
<!--
每屏 v0/Lovable 提示词模板，由 vj-ui-mock Phase B 生成。
落盘：docs/reference/research/designs/{epic-id}/{story-id}-{page}.prompt.md
头部 [BASE] 整段拷贝自 design_guidelines.md 的 ## v0 Base Prompt（不改写）。
-->

# UI 提示词：{屏名}（{路由}）

> 来源：Epic {epic-id} / Story {story-ids}　角色：{role}
> 出图后把截图保存为同目录 `{story-id}-{page}.png`，并回填 Story 的「### 设计参考」表。

---

## 提示词（可直接粘贴进 v0 / Lovable）

```
{此处整段拷贝 design_guidelines.md 的 v0 Base Prompt 内容}

页面：{屏名}　路由：{路由}　使用角色：{role}

这个页面要包含：
- {元素 1（来自 Story 前端验收标准）}
- {元素 2}
- ...

必须画出的状态：
- 默认/有数据态：{描述}
- 空态：{无数据时显示什么}
- Loading 态：{若有后端/AI 调用}
- 错误态：{失败时显示什么 + 重试/兜底入口}

交互：
- {交互 1，如"点提交→未作答弹确认对话框"}
```

---

## 对应验收标准（来自 Story，供出图后自查）

- {逐条列出该屏关联的 `#### 前端验收标准` AC 行}
````

- [ ] **Step 2: 验证模板**

检查清单：
- [ ] 顶部 HTML 注释写明落盘路径、`{story-id}-{page}.png` 命名、[BASE] 拷贝规则
- [ ] 含可粘贴的 fenced 提示词块，块内第一部分是 Base Prompt 占位、其后是 页面/路由/角色 + 元素 + 四态 + 交互
- [ ] 末尾「对应验收标准」区，回链 Story FE-AC（供出图后自查与 story-verify-fix 对齐）
- [ ] 命名 token 用 `{story-id}-{page}`，与 ai-workflow.md §5 既有约定一致（不造 `{screen}`）

- [ ] **Step 3: Commit**

```bash
git add .agents/skills/vj-ui-mock/v0-prompt.template.md
git commit -m "feat(vj-ui-mock): add per-screen v0 prompt template for Phase B"
```

---

## Task 3: SKILL.md（skill 主体）

**Files:**
- Create: `.agents/skills/vj-ui-mock/SKILL.md`

- [ ] **Step 1: 写 SKILL.md**

完整内容：

````markdown
---
name: vj-ui-mock
description: 为前端项目产出全局设计基座与每屏 v0/Lovable 提示词。Phase A 从 PRD 与全部 Epic/Story 生成 docs/project/design_guidelines.md（信息架构、导航骨架、设计系统、三态约定）；Phase B 把指定的 Epic/Story 归并成屏清单，每屏生成继承基座的 UI 提示词并回填 Story 设计参考。适用于已有 Epic/Story、准备进入前端实现、需要保证跨 Epic UI 衔接一致的场景。不直接生成图片，不做技术选型，不写前端代码。
---

# vj-ui-mock

补上 AI 工作流缺失的「UI 设计生产端」。链路位置：

```
PRD -> vj-architecture -> vj-epic-story -> 【vj-ui-mock】-> run-story
```

## 核心模型：两阶段

「UI mock」分两层，会 churn 的和需要全局的不是同一层：

- **设计基座（Phase A，全局一次）**：信息架构、导航骨架、设计系统、三态约定。变化频率低。**跨 Epic 衔接靠这层。**
- **逐屏详细稿（Phase B，按需）**：某屏的具体提示词。随实现节奏出，churn 限制在此层。

衔接保证：每屏提示词都拷贝同一段 `v0 Base Prompt`，epic2 的屏自动套 epic1 的外壳与 tokens。

## 适用 / 不适用

适用：
- 已有 Epic/Story（`docs/tasks/epics/`），准备做前端
- 想先有全局设计基座，再逐屏出 v0/Lovable 提示词
- 关心跨 Epic 的 UI 一致性

不适用：
- 还没有 Epic/Story（先用 `vj-product-requirements` / `vj-epic-story` / `vj-feature`）
- 要直接出图片（本 skill 只产提示词，出图交给 v0/Lovable 等工具）
- 要做后端/系统架构（用 `vj-architecture`）或写前端代码（用 `do-story`）

## 不做（边界）

- 不直接生成图片
- 不碰技术选型（栈从 CLAUDE.md 读；前端栈只在 design_guidelines.md 里冗余快照，方便提示词自包含）
- 不写前端代码
- v1 不做：跨 Epic 流程图、屏间跳转建模、屏智能去重、多套主题对比

## 输入

- `target`（Phase B 用）：接受任意组合
  - `epic-4` 或 epic 文件名 -> 该 Epic 全部屏
  - `4.2` -> 该 Story 涉及的屏
  - 多个混合（空格/逗号分隔），或 `all`
- 无 target -> 默认走 Phase A（或在 Phase A 已存在时询问要对哪个 Epic 跑 Phase B）

## Phase A：生成设计基座（全局，一次）

目标：产出 `docs/project/design_guidelines.md`。

1. **加载模板**：读取本 skill 目录的 `design_guidelines.template.md`。
2. **检测已有**：若 `docs/project/design_guidelines.md` 已存在 -> 进入增量更新（保留已确认内容，只补/改受影响节），不全量覆盖。
3. **读输入**：
   - `docs/project/requirements.md`（若存在）：取角色定义、核心价值、非功能里的 UI 相关约束（空态/loading/失败）
   - `docs/tasks/epics/` 全部 Epic：枚举所有屏（见「屏归并规则」）
   - `CLAUDE.md`：前端栈快照（§1）
   - `docs/project/architecture.md`（若存在）：仅取与前端相关的约束，不复制后端架构
4. **填 6 节**：信息架构（§2 站点地图用屏归并结果）、导航外壳（§3）、tokens（§4）、共享组件+三态（§5，结合 PRD 非功能要求）、角色 UX（§6，结合 PRD 角色边界）。每节短写。
5. **写 `## v0 Base Prompt` 段**：把 §1/§3/§4/§5 压成一段工具中立自然语言，以"请...设计下面这个具体页面："收口。
6. **[BLOCKING] 用户确认**：展示 design_guidelines.md 草案（尤其 §3 导航外壳 + v0 Base Prompt 段），用户确认后写入 `docs/project/design_guidelines.md`。

## Phase B：逐屏提示词（按 target，按需）

目标：对 target 涉及的每个屏产出一份 v0/Lovable 提示词。

1. **前置检查**：`docs/project/design_guidelines.md` 不存在 -> **停止并提示先跑 Phase A**，不静默生成基座。
2. **解析 target -> Epic/Story 集合**：把 `epic-N` / `N.M` / 混合 / `all` 解析到 `docs/tasks/epics/` 下的具体 Story。
3. **屏归并**（见下规则）：扫范围内每个 Story 的 `#### 前端验收标准`，按路由归并成屏；去重。
4. **缺口反馈**：范围内有 UI 但缺 `#### 前端验收标准` 的 Story -> 显式列出（成为补 Story 的信号），不静默跳过。
5. **默认引导单 Epic**：若 target 跨多个 Epic，提示"建议出一个、实现一个、再出下一个"，确认后再批量。
6. **逐屏生成**：用 `v0-prompt.template.md`，头部整段拷贝 design_guidelines.md 的 `v0 Base Prompt` 段，填入本屏 路由/角色/元素/四态/交互 与关联 AC。
7. **落盘**：`docs/reference/research/designs/{epic-id}/{story-id}-{page}.prompt.md`（目录不存在则创建）。
8. **回填 Story**：在对应 Story 的 `### 设计参考` 区登记该屏提示词与预期截图路径 `{story-id}-{page}.png`。
9. **告知用户**：列出生成的屏 + 提示词路径 + 下一步（把提示词粘进 v0/Lovable，出图回灌为同名 .png）。

### 屏归并规则

- 一个「屏」= 一个路由（page）。
- 来源：Story 的 `#### 前端验收标准` 里 `验证: Browser <path/selector/action> -> ...` 中的路由 `<path>`。
- 同一路由跨多个 Story 的 AC -> 合并到同一屏（元素/状态取并集）。
- 角色：取 Story 用户故事中的角色 / 该 Epic 的角色边界。
- 路由里的动态段（如 `/exam/{id}`）按模板屏处理，不为每个实例建屏。

## 产出物

- `docs/project/design_guidelines.md`（Phase A）
- `docs/reference/research/designs/{epic-id}/{story-id}-{page}.prompt.md`（Phase B，每屏一份）
- 回填到各 Story 的 `### 设计参考`

## 与其他 skill 的关系

- `vj-epic-story` / `vj-feature`：上游，产出 Epic/Story（vj-ui-mock 的输入来源）
- `vj-architecture`：边界——它管后端/系统架构与技术选型；vj-ui-mock Phase A 只管视觉/IA/导航
- `do-story`：下游，消费 `### 设计参考` 里的设计图实现前端
- `story-verify-fix`：下游，视觉对齐用的就是回填进 Story 的同名截图
- `documentation-pipeline:ln-114`：也产 design_guidelines.md（偏通用/WCAG）；本 skill 自产不依赖它，因为需要 `v0 Base Prompt` 段与本 repo epic/story 格式的屏站点地图

## 触发示例

```text
/vj-ui-mock                 # 先出全局设计基座（Phase A）
/vj-ui-mock epic-1          # 出 epic-1 所有屏的提示词（Phase B）
/vj-ui-mock 4.2             # 只出 Story 4.2 涉及的屏
/vj-ui-mock epic-1 epic-2   # 多 Epic（会提示按需，默认引导单 Epic）
```
````

- [ ] **Step 2: 验证 SKILL.md**

检查清单：
- [ ] frontmatter 仅 `name` + `description`，description 一句话覆盖两阶段 + 适用场景 + 三条边界（与现有 vj-* 风格一致）
- [ ] 含 Phase A（6 步，含 BLOCKING 用户确认）与 Phase B（9 步，含前置检查 / 屏归并 / 缺口反馈 / 默认单 Epic 引导）
- [ ] 含「屏归并规则」（路由=屏、跨 Story 取并集、动态段按模板屏）
- [ ] Phase B step 1 明确"缺 design_guidelines.md 则停止提示先跑 Phase A"
- [ ] 落盘路径与 Task 2 模板一致（`{story-id}-{page}.prompt.md`）
- [ ] 「与其他 skill 关系」含 ln-114 不依赖的说明

- [ ] **Step 3: Commit**

```bash
git add .agents/skills/vj-ui-mock/SKILL.md
git commit -m "feat(vj-ui-mock): add skill with Phase A/B UI mock workflow"
```

---

## Task 4: 在 skill-rules.json 注册 vj-ui-mock

**Files:**
- Modify: `.agents/skills/skill-rules.json`

- [ ] **Step 1: 读取现状**

Run: `python3 -c "import json; d=json.load(open('.agents/skills/skill-rules.json')); print(list(d['skills'].keys()))"`
Expected: 列出 7 个 skill（含 `vj-feature`、`reference-impl`、`frontend-dev-guidelines`）。确认 JSON 可正常解析。

- [ ] **Step 2: 追加 vj-ui-mock 条目**

在 `skills` 对象内（建议紧跟 `vj-feature` 之后）插入。注意保持前一条目结尾逗号正确：

```json
        "vj-ui-mock": {
            "type": "domain",
            "enforcement": "suggest",
            "priority": "medium",
            "description": "为前端项目产出全局设计基座（design_guidelines.md）与每屏 v0/Lovable 提示词，保证跨 Epic UI 衔接一致",
            "promptTriggers": {
                "keywords": [
                    "ui mock",
                    "ui设计",
                    "界面设计",
                    "设计稿",
                    "设计基座",
                    "原型图",
                    "v0",
                    "lovable",
                    "design guidelines",
                    "design_guidelines",
                    "提示词出图",
                    "前端设计"
                ],
                "intentPatterns": [
                    "(出|生成|做).*?(ui|界面|设计稿|原型|mock)",
                    "(v0|lovable).*?(提示词|prompt)",
                    "(全局|统一).*?(设计|ui|风格)",
                    "design.?guidelines"
                ]
            }
        }
```

- [ ] **Step 3: 验证 JSON 合法且条目生效**

Run:
```bash
python3 -c "import json; d=json.load(open('.agents/skills/skill-rules.json')); assert 'vj-ui-mock' in d['skills']; assert d['skills']['vj-ui-mock']['enforcement']=='suggest'; print('OK, skills count =', len(d['skills']))"
```
Expected: `OK, skills count = 8`（无 JSON 解析错误，无 trailing-comma 报错）

- [ ] **Step 4: Commit**

```bash
git add .agents/skills/skill-rules.json
git commit -m "feat(vj-ui-mock): register skill activation triggers"
```

---

## Task 5: 把 vj-ui-mock 接入 ai-workflow.md

**Files:**
- Modify: `docs/reference/guides/ai-workflow.md`

> 只更新 ai-workflow.md（设计链文档）。不改 CLAUDE.md：其「AI Workflow Entry Points」只列 Story 实现链 skill，不含设计链（vj-product-requirements/architecture/epic-story 都不在），vj-ui-mock 同属设计链，保持一致不进入该列表。

- [ ] **Step 1: §2 阶段 A 加入 vj-ui-mock**

定位（约 50-56 行）：
```text
vj-product-requirements
-> vj-architecture
-> vj-epic-story
```
改为：
```text
vj-product-requirements
-> vj-architecture
-> vj-epic-story
-> vj-ui-mock（前端项目：先 Phase A 出全局设计基座，再按 Epic 出每屏 v0 提示词）
```

- [ ] **Step 2: 改写 §5「前端设计图怎么接入」开头，点明生产端**

定位 §5 标题下首段（约 204-211 行，`do-story` 对 UI 设计图的发现顺序...）之前，插入一段：
```text
设计图的**生产**用 `vj-ui-mock`：

- Phase A 产出全局设计基座 `docs/project/design_guidelines.md`（信息架构 / 导航骨架 / 设计系统 / 三态约定）
- Phase B 按 Epic/Story 产出每屏 v0/Lovable 提示词，落盘到 `docs/reference/research/designs/{epic-id}/{story-id}-{page}.prompt.md`，并回填 Story 的 `### 设计参考`
- 你把提示词粘进 v0/Lovable 出图后，将截图保存为同目录 `{story-id}-{page}.png`

下面是 `do-story` 对 UI 设计图的**发现**顺序：
```
（保留其后原有的发现顺序列表与命名约定不动。）

- [ ] **Step 3: §9 一句话决策表加一行**

定位 §9 表格，在「只有产品想法...」一行下方加：
```text
| 已有 Epic/Story，要出前端设计基座与每屏提示词 | `vj-ui-mock` |
```

- [ ] **Step 4: §10 推荐默认顺序，需求未成型块加 vj-ui-mock**

定位 §10：
```text
需求未成型:
  vj-product-requirements
  -> vj-architecture
  -> vj-epic-story
```
改为：
```text
需求未成型:
  vj-product-requirements
  -> vj-architecture
  -> vj-epic-story
  -> vj-ui-mock（前端项目，可选）
```

- [ ] **Step 5: 验证改动**

Run: `grep -n "vj-ui-mock" docs/reference/guides/ai-workflow.md`
Expected: 至少 4 处命中（§2、§5、§9、§10）。

通读这 4 处，确认：
- [ ] §2 / §10 把 vj-ui-mock 放在 vj-epic-story 之后、标注"前端项目/可选"
- [ ] §5 明确 vj-ui-mock 是设计图「生产端」，且**未删除**原有「发现顺序」内容
- [ ] §9 决策表新行描述准确

- [ ] **Step 6: Commit**

```bash
git add docs/reference/guides/ai-workflow.md
git commit -m "docs(ai-workflow): wire vj-ui-mock into the design chain"
```

---

## Task 6: 端到端 dry-run 验证（对照 spec §11 验收标准）

不产新文件，纯验证。用真实 epic 跑通 skill 逻辑，逐条核对 spec §11。

**Files:**
- 只读：`.agents/skills/vj-ui-mock/SKILL.md`、`docs/tasks/epics/epic-1-employee-identity-login.md`、`docs/tasks/epics/epic-4-exam-assembly-taking.md`、`docs/project/requirements.md`、`CLAUDE.md`

- [ ] **Step 1: dry-run Phase A**

按 SKILL.md Phase A 流程，对全部 6 个 epic + requirements.md + CLAUDE.md 走一遍（不写文件，口头产出草案），核对：
- [ ] §2 站点地图能从各 Story `#### 前端验收标准` 枚举出屏（至少 `/login`、`/admin/papers/new`、`/exam/{id}` 等）
- [ ] §1 前端栈快照取自 CLAUDE.md（MUI v9 等）
- [ ] §5 三态约定能对应 PRD §4.2 的空态 / AI 进行中 / 失败兜底
- [ ] §6 角色 UX 能对应 PRD §2 的出题管理员 / 员工考生边界
- [ ] 能产出收口正确的 `v0 Base Prompt` 段

- [ ] **Step 2: dry-run Phase B（epic-1）**

`target = epic-1`，核对：
- [ ] 解析出 Story 1.1（及 epic-1 其它 Story）
- [ ] 屏归并：从 1.1 的前端验收标准得到 `/login` 屏（元素：账号选择/输入控件、提交按钮；交互：登录成功跳首页显示用户名）
- [ ] 生成的提示词头部 = design_guidelines.md 的 v0 Base Prompt 段整段
- [ ] 落盘路径 = `docs/reference/research/designs/1/1.1-login.prompt.md`（命名符合约定）
- [ ] 末尾「对应验收标准」回链到 1.1 的 FE-AC

- [ ] **Step 3: dry-run 边界场景**

- [ ] `target = 4.2`（单 Story）：归并出 `/exam/{id}` 屏，动态段按模板屏处理（不为每个 id 建屏）
- [ ] `target = epic-1 epic-2`（多 Epic）：触发"默认引导单 Epic"提示
- [ ] **缺 design_guidelines.md 跑 Phase B**：停止并提示先跑 Phase A（不静默生成）
- [ ] 构造一个"有 UI 但无 `#### 前端验收标准`"的 Story（假想）：被显式列入缺口反馈

- [ ] **Step 4: 逐条核对 spec §11 验收标准**

对 spec `docs/superpowers/specs/2026-06-01-vj-ui-mock-design.md` §11 的 8 条 checkbox，逐条确认本实现满足。任一不满足 -> 回到对应 Task 修正。

- [ ] **Step 5: 最终 commit（如 dry-run 触发了修正）**

```bash
git add -A
git commit -m "fix(vj-ui-mock): adjustments from end-to-end dry-run"
```
若 dry-run 无需修正，跳过本步。

---

## Self-Review（计划作者已执行）

- **Spec 覆盖**：§1 定位→Task3 SKILL；§2 两阶段→Task3 Phase A/B；§3 6 节→Task1 模板 + Task3 Phase A step4-5；§4 输入模型/屏归并→Task3 输入+屏归并规则；§5 落盘回填→Task2 + Task3 step7-8；§6 前置依赖→Task3 Phase B step1；§7 边界→Task3「与其他 skill 关系」+ Task5 不改 CLAUDE.md 说明；§8 不依赖 ln-114→Task3；§9 YAGNI→Task3「不做」；§11 验收→Task6 全覆盖。无遗漏。
- **占位符扫描**：模板内的 `<...>` / `{...}` 是模板占位符（合理，非计划占位），计划步骤本身均给出完整内容与命令。
- **类型/命名一致**：落盘 token `{story-id}-{page}` 在 spec §5、Task2 模板、Task3 SKILL、Task6 dry-run 全一致；`v0 Base Prompt` 段名在 Task1/Task2/Task3 一致。
